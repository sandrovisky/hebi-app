import 'package:flutter/material.dart';

import './/ui/containers/containers.dart';

class JBRouter extends StatelessWidget {
  const JBRouter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const JBContainer();
  }
}
