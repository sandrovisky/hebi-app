import 'package:flutter/material.dart';

import './/ui/containers/containers.dart';

class ConfigRouter extends StatelessWidget {
  const ConfigRouter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const ConfigContainer();
  }
}
